{
    "options": [
        {
            "name": "distribution",
            "type": "select",
            "options": [
                "normal",
                "gamma"
            ]
        }
    ]
}