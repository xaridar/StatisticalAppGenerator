{
    "options": [
        {
            "name": "distribution",
            "description": "testing!",
            "type": "select",
            "options": [
                "normal",
                "gamma"
            ]
        }
    ]
}